type: object
properties:
  error:
    type: string
    description: Error type
    example: "CONFLICT"
  message:
    type: string
    description: Error message
    example: "Order already paid"
required:
  - error
  - message
